name: Package

on:
  push:
    branches:    
    - master
jobs:
  build:
    runs-on: ubuntu-16.04
    steps:
      - uses: actions/checkout@v1
      - name: package
        uses: ./.github/actions/package@local
      
         
